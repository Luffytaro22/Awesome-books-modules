name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código fuente
        uses: actions/checkout@v2

      - name: Instalar dependencias
        run: npm install

      - name: Construir aplicación
        run: npm run build

      # Agrega aquí los pasos adicionales para realizar el despliegue de tu aplicación
